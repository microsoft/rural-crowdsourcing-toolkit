{
  "name": "@karya/db",
  "version": "2.0.0",
  "description": "Contains specification of Karya DB Schema, and appropriate parsers and generators",
  "private": true,
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist* && rm -rf tsconfig*.tsbuildinfo",
    "compile": "tsc -b tsconfig-local.json && node ./dist-local/GenFiles.js && tsc -b tsconfig.json"
  },
  "license": "MIT",
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "@types/prettier": "^2.2.3",
    "@types/uppercamelcase": "^3.0.0",
    "camelcase": "^6.2.0",
    "fs": "^0.0.1-security",
    "prettier": "^2.2.1",
    "typescript": "^4.2.3",
    "uppercamelcase": "^3.0.0",
    "yaml": "^1.10.2"
  }
}
