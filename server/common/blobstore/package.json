{
  "name": "@karya/blobstore",
  "version": "2.0.0",
  "description": "This package implements blobstore related functionality for Karya",
  "private": true,
  "license": "MIT",
  "main": "dist/Index",
  "types": "dist/Index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.33",
    "@types/node": "^14.14.35",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.5.0",
    "bluebird": "^3.7.2"
  }
}
