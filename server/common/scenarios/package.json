{
  "name": "@karya/scenarios",
  "version": "2.0.0",
  "description": "This package implements all scenario specific functionality",
  "private": true,
  "license": "MIT",
  "main": "dist/index",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "^4.2.3"
  },
  "dependencies": {
  }
}
