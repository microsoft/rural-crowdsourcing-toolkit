{
  "name": "@karya/misc-utils",
  "version": "2.0.0",
  "description": "This package implements a few misc utils for the backend and the box server.",
  "private": true,
  "license": "MIT",
  "main": "dist/Index",
  "types": "dist/Index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "devDependencies": {
    "@types/crypto-js": "^4.0.2",
    "@types/node": "^14.14.35",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "crypto-js": "^4.1.1"
  }
}
