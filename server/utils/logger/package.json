{
  "name": "@karya/logger",
  "version": "2.0.0",
  "description": "This package implements a wrapper around the winston logger",
  "private": true,
  "license": "MIT",
  "main": "dist/Index",
  "types": "dist/Index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "moment": "^2.29.1",
    "winston": "^3.3.3",
    "winston-daily-rotate-file": "^4.5.1"
  }
}
