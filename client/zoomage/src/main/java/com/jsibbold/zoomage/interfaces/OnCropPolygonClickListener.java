package com.jsibbold.zoomage.interfaces;

import com.jsibbold.zoomage.dataClass.Polygon;
import com.jsibbold.zoomage.dataClass.PolygonData;

public interface OnCropPolygonClickListener {
    void onClick(PolygonData polygonData);
}
